# Using the gemspec command here causes test_internals to be included,
# which means it attempts to run as a test when rake is called.

source 'http://rubygems.org'

gem 'app_mode', '~> 1.0.0'

group :rake do
  gem 'rake_tasks', '~> 1.0.0'
end
